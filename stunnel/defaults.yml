# -*- coding: utf-8 -*-
# vim: ft=yaml
# Defaults for stunnel

stunnel:
  enabled: False
  package: stunnel4
  conf_dir: /etc/stunnel
  default: /etc/default/stunnel4
  service:
    name: stunnel4
    state: dead
    enable: False
  config:
    user: stunnel4
    group: stunnel4
